<div class="home">
    <h1>Criar nova mensagem no mural</h1>    
    <div class="authcontainer">
        <form action="/mural/criaMural" method="post">
            <div class="formulario">
                <label for="titulo">Insira o título do aviso/circular: </label>
                <input type="text" name="titulo" placeholder="digite o título do aviso">
            </div>
            <div class="formulario">
                <label for="texto">Digite a mensagem da circular: </label>
                <textarea name="texto" rows="14">digite a mensagem da circular</textarea>
            </div>
            <div class="formulario">
                <label for="cpf">Digite o CPF do responsável pelo aviso: </label>
                <input type="texto" name="cpf" value="{{admin.cpf}}">
            </div>
            <div class="formulario">
                <label for="nome">Digite o nome do responsável: </label>
                <input type="texto" name="nome" value="{{admin.nome}}">
            </div>
            <div class="formulario">
                <button type="submit">Cadastrar</button>
            </div>
        </form>
    </div>
</div>